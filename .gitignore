node_modules/
.env
.worktrees/
.parallel-task.json
.DS_Store
pickleball.db
PROJECT_NOTES.local.md
PROJECT_NOTES.local.md.backup-*
# Backups written by scripts/cleanup-test-games.js before it deletes anything
deleted-test-games-*.json
# Scratch file used to hand a rotated DB password over without pasting it into chat
.new-db-password.local
# Running record of Textbelt quota readings, written by npm run quota
.textbelt-quota-log

# Generated documentation - rebuild any time with `npm run docs`.
# The scripts are tracked; the output is not, because the screenshots are several MB per run and
# would bloat the history every time the UI changes. docs/README.md IS tracked.
docs/*.html
docs/screens/
docs/screens-phone/
