# Claude Code Project Instructions

Follow every project instruction in @AGENTS.md.

## Claude-Specific Supplemental Context

Do not republish the older Claude artifact copies listed in `docs/ARTIFACTS.md` unless Scott
asks for them specifically. The current documentation lives in `/dev.html` → Screens and is
published by the shared completion workflow.

The password-protected `/dev.html` area is Scott's operational dashboard. Its idea board records
work that is unfinished or finished but not deployed, and its browser error reporting originates
in `public/js/header.js`. Keep that reporter fail-silent because it runs on player-facing pages.
